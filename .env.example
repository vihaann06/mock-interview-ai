# Environment variables for Probe MVP

# OpenAI-compatible interviewer (required for live chat)
OPENAI_API_KEY=
# Optional override; default gpt-4o-mini
OPENAI_MODEL=gpt-4o-mini
# Optional base URL for OpenAI-compatible providers
# OPENAI_BASE_URL=https://api.openai.com/v1


# Deepgram (voice STT via Flux) — server-only; never expose to the browser.
# Used by POST/GET /api/deepgram/token to mint short-lived JWTs for Flux WS auth.
DEEPGRAM_API_KEY=
# Optional TTS voice for OpenAI audio (default alloy)
# OPENAI_TTS_VOICE=alloy
