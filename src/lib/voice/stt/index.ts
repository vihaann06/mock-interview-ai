/**
 * TEMP — Agent2 placeholder until Agent1 merges real Deepgram Flux STT.
 * Replace this module with the production provider; keep `createDeepgramFluxSTT` export name.
 */
export { createDeepgramFluxSTT } from "./temp-mock-provider";
